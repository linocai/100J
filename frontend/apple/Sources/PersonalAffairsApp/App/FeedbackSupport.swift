import Foundation

enum FeedbackSupport {
    static let email = "linotsai@100j.app"
    static let issuesURL = URL(string: "https://github.com/linocai/100J/issues")!
    static let mailURL = URL(string: "mailto:\(email)")!
}
